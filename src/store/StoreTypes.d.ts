// here we'll define types of data we store in the app state

// it's an example state as a hint for other teammates
// to keep the defined structure and can be removed once one is defined

export interface IExampleState {
  value: number;
}

// current app types
